import React, { memo } from 'react';

type {{ pascalCase name }}Props = {

}

const {{ pascalCase name }} : React.FC<{{ pascalCase name }}Props> = memo(({}) => {
  return (
    <div>
        {{ pascalCase name }}
    </div>
  );
});

export default {{ pascalCase name }};
